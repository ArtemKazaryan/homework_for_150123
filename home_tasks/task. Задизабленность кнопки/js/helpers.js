/* helpers.js */
export const openSidebar = () => {
	// my code
    const element = document.querySelector("#app-sidebar");
    element.classList.add("show")
}

// В навигации приложения есть кнопка `Sidebar`. Когда вы нажимаете на нее, вызывается функция `openSidebar`. Завершите функцию `openSidebar`, чтобы она добавила класс `show` к элементу с идентификатором `app-sidebar`. 

// В `style.css` есть класс `.show`, который добавляет стили для сайдбара в открытом состоянии.